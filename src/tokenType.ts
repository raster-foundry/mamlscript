enum TokenType {
  Identifier,
  Constant,
  AssociativeMapping,
  Operator,
  GroupingStart,
  GroupingEnd,
  BlockStart,
  BlockEnd,
  Whitespace,
  ArgumentDelimiter,
  ExpressionEnd
}

export default TokenType;
